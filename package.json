{
  "name": "youtube-apollo-server",
  "version": "1.0.0",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "yarn run build && ts-node build/index.js",
    "start": "ts-node src/index.ts",
    "deploy": "yarn run build && gcloud app deploy"
  },
  "dependencies": {
    "apollo-datasource-rest": "^0.13.0",
    "apollo-server": "^2.24.0",
    "graphql": "^15.5.0",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
